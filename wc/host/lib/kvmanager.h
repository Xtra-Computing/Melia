#include <kvbuffer.h>

bool insert_to_kvbuffer(unsigned short key_size, void *key, unsigned short value_size, void *value, kvbuffer *buffer, __local unsigned int *index_offset, local unsigned int *memory_offset)
{
	
}