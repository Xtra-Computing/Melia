#include "lib\rog.h"

/*tempararily, we use sequential scan*/
scan(global Reduction_Object_G *object, global unsigned int *key_start_per_bucket, 
global unsigned int *value_start_per_bucket, global unsigned int *pair_start_per_bucket)
{
	
}